group 'com.untitledsurvival.lib.plugin'
version '1.0'

processResources {
    filesMatching('plugin.yml') {
        expand(
                name: project.name,
                version: version,
                group: project.group
        )
    }
}
